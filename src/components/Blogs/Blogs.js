import React from 'react';

const Blogs = () => {
    return (
        <div className='container'>
            <h1 className='banner-title'>Blogs Loading...</h1>
        </div>
    );
};

export default Blogs;